"""
Taken in most parts from https://github.com/NVlabs/MUNIT/blob/1466a009b7bc989cbc3f9c6e6734e506132bf680/data.py
"""

import os

from PIL import Image
from torch.utils import data

from pytorch_training.data.utils import default_loader, make_dataset


class ImageFolder(data.Dataset):

    def __init__(self, root, transform=None, return_paths=False,
                 loader=default_loader):
        imgs = sorted(make_dataset(root))
        if len(imgs) == 0:
            raise(RuntimeError("Found 0 images in: " + root + "\n"
                               "Supported image extensions are: " +
                               ",".join(Image.EXTENSION.keys())))

        self.root = root
        self.imgs = imgs
        self.transform = transform
        self.return_paths = return_paths
        self.loader = loader

    def __getitem__(self, index):
        path = self.imgs[index]
        img = self.loader(path)
        if self.transform is not None:
            img = self.transform(img)
        if self.return_paths:
            return img, path
        else:
            return img

    def __len__(self):
        return len(self.imgs)
